import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  // Disable Turbopack for production build (more stable)
  // Turbopack is used in dev via npm run dev --turbopack
}

export default nextConfig
